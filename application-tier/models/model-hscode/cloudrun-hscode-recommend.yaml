apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: hscode-recommend-service
  annotations:
    run.googleapis.com/ingress: all
    # run.googleapis.com/execution-environment 제거!
spec:
  template:
    metadata:
      annotations:
        # 여기로 이동!
        run.googleapis.com/execution-environment: gen2
        autoscaling.knative.dev/maxScale: "10"
        autoscaling.knative.dev/minScale: "1"
        run.googleapis.com/cpu-throttling: "false"
        run.googleapis.com/memory: "2Gi"
        run.googleapis.com/cpu: "1000m"
    spec:
      containerConcurrency: 80
      timeoutSeconds: 300
      containers:
      - name: hscode-recommend-container
        image: asia-northeast3-docker.pkg.dev/customs-clearance-system/docker-repo/hscode-recommender:latest
        ports:
        - name: http1
          containerPort: 8080
        env:
        - name: ENVIRONMENT
          value: "production"
        - name: BUILD_VERSION
          value: "v3"
        - name: SERVICE_NAME
          value: "HS Code Recommendation Service"
        - name: UV_SYSTEM_PYTHON
          value: "1"
        - name: LOG_LEVEL
          value: "INFO"
        - name: ENABLE_DOCS
          value: "true"
        - name: PYTHONPATH
          value: "/app"
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              key: latest
              name: openai-api-key
        resources:
          limits:
            cpu: 1000m
            memory: 2Gi
        command: ["/bin/sh"]
        args: ["-c", "uv run uvicorn app.main:app --host 0.0.0.0 --port ${PORT}"]
  traffic:
  - percent: 100
    latestRevision: true
