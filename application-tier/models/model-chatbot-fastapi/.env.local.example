# 로컬 개발용 환경변수 예시
# 이 파일을 .env로 복사하고 실제 값으로 채워넣으세요

# 🏠 로컬 환경 설정
ENVIRONMENT=development

# 🐘 PostgreSQL 설정 (Docker 컨테이너)
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_DB=conversations
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password

# 🔴 Redis 설정 (Docker 컨테이너)
REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_PASSWORD=
REDIS_DB=0

# ⚙️ 연결 풀 설정 (로컬 환경용)
POSTGRES_POOL_SIZE=10
POSTGRES_MAX_OVERFLOW=20
REDIS_MAX_CONNECTIONS=20

# 🤖 OpenAI API 설정 (필수)
OPENAI_API_KEY=sk-your-openai-api-key-here

# 📚 ChromaDB 설정 (Docker 컨테이너)
CHROMADB_MODE=docker
CHROMADB_HOST=localhost
CHROMADB_PORT=8011
CHROMADB_USE_SSL=false

# 🔧 기타 설정
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1

# 🧪 개발/디버깅용
LOG_LEVEL=DEBUG
DEBUG=true
HOST=0.0.0.0
PORT=8004