# ChromaDB Docker 연결을 위한 환경변수 설정 예시
# 이 파일을 .env로 복사하여 사용하세요

OPENAI_API_KEY=your_openai_api_key_here

HF_TOKEN=your_huggingface_token_here

# ChromaDB 연결 모드 설정
CHROMADB_MODE=docker

# ChromaDB Docker 서버 설정
CHROMADB_HOST=localhost
CHROMADB_PORT=8011

# 주의사항:
# 1. CHROMADB_MODE=local로 설정하면 기존 로컬 파일 기반 ChromaDB 사용
# 2. CHROMADB_MODE=docker로 설정하면 data-tier의 ChromaDB 컨테이너와 연결
# 3. Docker 모드 사용 시 data-tier에서 ChromaDB 컨테이너가 실행 중이어야 함
# 4. 포트 8011이 data-tier/docker-compose.yml의 ChromaDB 포트와 일치해야 함